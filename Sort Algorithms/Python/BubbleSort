# Algorithm:1
# Bubble Sort
# Author:--Green--Arrow-->


Size=int(input("Enter the Size-->"))                               # Defines the Size of the Array
List=[]                                                            #Creates an Empty List To store Values
for item in range(Size):
    List.append(int(input("Enter the Value-->")))                  #Takes the input from the user in integer format

for i in range(Size-1):
    for j in range(Size-1):
        if List[j]>List[j+1]:                                      #If Element at next position is greater than current
            temp=List[j]                                           #Then Swap both elements.
            List[j]=List[j+1]
            List[j+1]=temp
print("Soterd Array-->"List)
